<div class="container">
<%= form_with model: [@category, @category.transactions.create]  do |form| %>
  <% if transaction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(transaction.errors.count, "error") %> prohibited this transaction from being saved:</h2>

      <ul>
        <% transaction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, class: "form-label", style: "display: block" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div>
    <%= form.label :amount, class: "form-label", style: "display: block" %>
    <%= form.number_field :amount, class: "form-control" %>
  </div><br>

  <div class="sign-buttons">
    <%= form.submit class: "btn btn-success", id: "btn-log" %>
  </div>
<% end %>
</div>
